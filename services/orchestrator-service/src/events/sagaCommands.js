// import { publishEvent } from "@expensio/sharedlib";
// import { EVENTS } from "@expensio/sharedlib";

// // Sends a command to delete the user
// export const sendDeleteUserCommand = async (userId, channel) => {
// 	const command = {
// 		userId,
// 	};
// 	await publishEvent(EVENTS.USER_DELETED_INITIATED, command, channel);
// };

// // Sends a command to delete the user's expenses
// export const sendDeleteExpensesCommand = async (userId, channel) => {
// 	const command = {
// 		userId,
// 	};
// 	await publishEvent(EVENTS.EXPENSE_DELETION_INITIATED, command, channel);
// };
