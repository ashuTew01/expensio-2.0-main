// import { logError, logInfo } from "@expensio/sharedlib";
// import {
// 	addSubscriptionToDb,
// 	assignSubscriptionToUser,
// } from "../services/aiSubscriptionService.js";

export const starterFunctions = async () => {
	// try {
	// 	const subscription = await addSubscriptionToDb("Guest", 0);
	// 	logInfo(`Subscription '${subscription.name}' created.`);
	// 	const userId = 0;
	// 	await assignSubscriptionToUser(userId, "67095ab0f22e6fa6b342d7eb");
	// 	logInfo(`Subscription 'Guest' assigned to userId: ${userId}.`);
	// } catch (error) {
	// 	logError(error);
	// }
};

// const uselessFunc = async () => {
//     logInfo(`Subscription '${subscription.name}' created.`);

//     const userId = 1;
//     await assignSubscriptionToUser(userId, subscription._id);
//     logInfo(
//         `Subscription '${subscription.name}' assigned to userId: ${userId}.`
//     );

//

// }
